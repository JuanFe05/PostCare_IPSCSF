const UnauthorizedPage = () => (
  <div className="flex flex-col items-center justify-center h-screen text-center text-yellow-700">
    <h1 className="text-3xl font-bold mb-4">No autorizado</h1>
    <p className="text-lg">No tienes permisos para acceder a esta sección.</p>
  </div>
);

export default UnauthorizedPage;
